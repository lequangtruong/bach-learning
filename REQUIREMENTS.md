# Bách Learning — Tài liệu tổng hợp yêu cầu

> Trạng thái: bản tổng hợp để duyệt trước khi đưa vào mục tiêu triển khai.
>
> Quy ước: mục **Đã yêu cầu** là phạm vi cần giữ; mục **Đề xuất** chưa được xem là quyết định cuối cùng; mục **Chưa chốt** cần xác nhận trước khi triển khai sâu.

## 1. Mục tiêu sản phẩm

Xây dựng một web app học tập cá nhân cho Bách, hiện vừa lên lớp 4, tập trung vào hai môn:

- **Toán:** bám chương trình sách giáo khoa lớp 3 và lớp 4, khắc phục lỗ hổng lớp 3, sau đó phát triển tư duy để từng bước giải được bài toán nâng cao, Olympic quốc gia và các dạng phù hợp với Singapore/Trung Quốc.
- **Tiếng Việt/Văn:** đọc hiểu, dùng từ, đặt câu, lập dàn ý và viết văn rõ ràng, mạch lạc, có lý lẽ/cảm xúc nhưng vẫn đúng độ tuổi; không ép văn phong người lớn.

Mục tiêu không phải nhồi bài khó ngay từ đầu mà là xây nền chắc, hình thành phương pháp suy nghĩ, biết tự kiểm tra và tăng dần độ khó.

## 2. Đối tượng và điểm xuất phát

- Học sinh tên Bách.
- Năm học hiện tại: bắt đầu lớp 4.
- Cần giả định một số kiến thức lớp 3 có thể chưa vững.
- Chương trình phải có bước **chẩn đoán đầu vào lớp 3**, ôn bù có chọn lọc rồi mới tăng tốc lớp 4.
- Nội dung, câu chữ, giao diện và phản hồi phải phù hợp với trẻ tiểu học.

## 3. Yêu cầu chương trình học

### 3.1. Cấu trúc chung

- Có lộ trình theo tuần, theo ngày và theo từng bước nhỏ.
- Mỗi bài học cần thể hiện rõ:
  1. Mục tiêu.
  2. Kiến thức/kỹ năng cần nhớ.
  3. Ví dụ mẫu.
  4. Bài tập cơ bản.
  5. Bài tập vận dụng.
  6. Bài thử thách hoặc bài suy luận.
  7. Cách tự kiểm tra và sửa lỗi.
  8. Tiêu chí hoàn thành.
- Có thể điều chỉnh nhịp học theo kết quả của Bách, không bắt buộc tiến độ cứng.
- Có theo dõi tiến bộ, lỗi lặp lại, điểm mạnh và nội dung cần học lại.
- Cần ưu tiên chất lượng phương pháp hơn số lượng bài.

### 3.2. Toán

Lộ trình cần đi theo hướng:

1. Kiểm tra và củng cố nền lớp 3.
2. Hoàn thiện kiến thức trọng tâm lớp 4.
3. Phát triển tư duy giải toán.
4. Tăng dần bài nâng cao/Olympic.

Các năng lực cần xây dựng:

- Tính toán chính xác, ước lượng và kiểm tra kết quả.
- Có nền tính toán nhanh và tính nhẩm chắc: gộp–tách số, bù trừ, cấu trúc phép nhân/chia, ước lượng và kiểm tra ngược; độ chính xác và giải thích luôn đi trước tốc độ, không phụ thuộc máy tính bỏ túi.
- Hiểu bản chất thay vì chỉ nhớ công thức.
- Đọc kỹ đề, tóm tắt dữ kiện, xác định câu hỏi.
- Biết vẽ sơ đồ đoạn thẳng, bảng, hình hoặc mô hình khi phù hợp.
- Nhận ra quy luật, quan hệ, bất biến và cách chia trường hợp.
- Giải toán nhiều bước, toán lời văn, phân số, đại lượng, hình học, tổ hợp và logic ở mức phù hợp.
- Trình bày lời giải đầy đủ, có lập luận và kiểm chứng.
- Với bài khó: ưu tiên gợi ý từng tầng, không đưa đáp án ngay.

Mức nâng cao cần tham khảo tinh thần bài toán Singapore/Trung Quốc và Olympic, nhưng phải có thang độ khó từ dễ đến khó, không biến chương trình lớp 4 thành chương trình luyện thi quá sức.

### 3.3. Tiếng Việt và Văn

Các năng lực cần xây dựng:

- Đọc hiểu đúng ý chính, chi tiết, trình tự, nguyên nhân/kết quả và thông điệp.
- Mở rộng vốn từ, dùng từ đúng nghĩa, đặt câu rõ.
- Nắm cấu trúc đoạn văn: câu chủ đề, câu phát triển, câu kết.
- Biết lập dàn ý trước khi viết.
- Viết câu và đoạn mạch lạc, liên kết tự nhiên.
- Viết các dạng bài phù hợp lớp 3–4: kể chuyện, tả người, tả cảnh, tả đồ vật, thuật việc, nêu cảm nghĩ và viết theo tranh/gợi ý.
- Biết tự đọc lại để sửa lỗi chính tả, dấu câu, lặp từ, thiếu ý và câu quá dài.
- Khuyến khích quan sát thật, cảm xúc thật và cách diễn đạt riêng.
- Phản hồi cần cụ thể, nhẹ nhàng, chỉ ra cách sửa; không viết thay toàn bộ bài của Bách.

## 4. Lộ trình dự kiến

Đề xuất ban đầu là lộ trình khoảng 36 tuần, chia thành các giai đoạn:

- **Giai đoạn 1:** chẩn đoán và nối nền lớp 3 → lớp 4.
- **Giai đoạn 2:** củng cố kiến thức cốt lõi lớp 4.
- **Giai đoạn 3:** luyện giải toán nhiều bước và viết đoạn/văn có cấu trúc.
- **Giai đoạn 4:** phát triển tư duy nâng cao, chiến lược giải và diễn đạt.
- **Giai đoạn 5:** bài tổng hợp, sửa lỗi cá nhân và tăng độ bền.
- **Giai đoạn 6:** thử thách Olympic có chọn lọc, tổng kết và định hướng tiếp theo.

Lộ trình phải bám chương trình sách giáo khoa lớp 3 + lớp 4, không tách rời hoàn toàn khỏi chương trình chính khóa.

## 5. Yêu cầu web app và thiết bị

- Chạy dưới dạng web app trên `vercel.app`.
- Mở tốt trên Safari iPad, ưu tiên trải nghiệm cảm ứng.
- Không bắt buộc cài app native.
- Giao diện nhẹ, rõ ràng, ít thao tác, phù hợp trẻ em và phụ huynh.
- **Typography tiếng Việt là yêu cầu thiết kế bắt buộc:** phải dùng font có hỗ trợ đầy đủ dấu tiếng Việt; dành line-height và khoảng đệm theo chiều cao thực của chữ có dấu, không áp dụng line-height quá chặt theo giao diện tiếng Anh. Tiêu đề nhiều dòng, nhãn, nút và thẻ phải được thử trên Safari iPad để dấu không bị chạm/cắt và khoảng cách trên–dưới vẫn cân đối.
- Có chức năng **nhập văn bản bằng giọng nói** để Bách hoặc phụ huynh nói câu hỏi, câu trả lời, ghi chú và nội dung bài Văn.
- Có chức năng **đọc câu trả lời bằng giọng nói** ở mức nhẹ: dùng speech synthesis của trình duyệt, tiếng Việt, ưu tiên giọng nữ dịu; chỉ phát khi người dùng bấm nút và luôn giữ bản text làm fallback.
- Luôn có ô nhập bằng bàn phím làm phương án thay thế khi không cấp quyền micro, môi trường không hỗ trợ hoặc nhận dạng sai.
- Cần có nút bắt đầu/dừng ghi âm, trạng thái đang nghe, xem lại văn bản nhận dạng và cho phép sửa trước khi gửi.
- Thiết kế thao tác micro phù hợp màn hình cảm ứng iPad; không tự động bật micro khi chưa có thao tác rõ ràng của người dùng.
- Có thể dùng offline ở mức cơ bản nhờ bộ nhớ cục bộ trên iPad.
- Đồng bộ dữ liệu khi có mạng.
- Không phụ thuộc vào một server riêng phải tự vận hành nếu có thể tránh.

## 6. Lưu trữ dữ liệu

Nhu cầu chính là **lưu DB online**, không nhất thiết phải xây dựng hệ thống backend lớn.

Dữ liệu cần lưu tối thiểu:

- Hồ sơ và cấu hình học của Bách.
- Tiến độ theo tuần/bài.
- Kết quả bài tập.
- Các lỗi thường gặp.
- Bài viết và lịch sử phản hồi.
- Ghi chú của phụ huynh.
- Phiên bản dữ liệu và thời điểm đồng bộ.

Google Drive hiện có của người dùng được xem là nơi lưu DB online/backup. Cần phân biệt hai lựa chọn:

- `appDataFolder`: riêng tư, ẩn khỏi giao diện Drive, phù hợp cho DB ứng dụng.
- `drive.file`: file nằm trong Drive và người dùng có thể nhìn thấy/sao lưu thủ công.

**Đã chốt:** dùng file nằm trong Google Drive, người dùng có thể nhìn thấy và sao lưu thủ công; không dùng `appDataFolder`.

Phương án kỹ thuật đã chốt cho bản đầu:

- iPad: IndexedDB làm cache và hỗ trợ offline.
- Google Drive: lưu một file dữ liệu JSON hoặc định dạng tương đương.
- Khi đăng nhập: tải dữ liệu từ Drive.
- Khi thay đổi: ghi cục bộ trước, đồng bộ lên Drive khi có mạng.
- Với phạm vi một người dùng, có thể bắt đầu bằng quy tắc xung đột đơn giản; không cần hệ thống cộng tác nhiều người ở giai đoạn đầu.

Các điểm trên đã được người dùng đồng ý cho phạm vi bản đầu.

## 7. OAuth và AI

### 7.1. Nguyên tắc bắt buộc

- Không dùng Gemini API key trực tiếp trong browser.
- Người dùng muốn dùng **OAuth**, không phải API key.
- UX đăng nhập phải là nút mở luồng Google Sign-In/OAuth; không yêu cầu người dùng cuối nhập Google OAuth Web Client ID hoặc Client Secret. Client ID public là cấu hình của deployment.
- OAuth của Google Drive và OAuth của Gemini/AGY phải được thiết kế tách quyền rõ ràng.
- Cùng tài khoản Google có thể dùng, nhưng không mặc định dùng chung một access token.
- Không đưa credential nhạy cảm của AGY vào mã frontend hoặc repository.

### 7.2. Vai trò của Gemini/AGY

- **AI trợ giảng là thành phần bắt buộc của sản phẩm.** Bách cần có thể hỏi bài, xin gợi ý, trình bày lời giải, chữa bài Toán và nhận phản hồi bài Văn.
- **Đã chốt:** Gemini là AI trợ giảng online chính phục vụ người dùng.
- AGY sandbox dùng để thực hiện phần code vì nhanh; phải được giao brief thiết kế rõ ràng, không để AGY tự quyết định giao diện/kiến trúc ngoài phạm vi.
- AGY dùng để phát triển code bên ngoài app; không cần xây chức năng để Bách gửi yêu cầu sửa mã nguồn trực tiếp trong ứng dụng.
- Tham khảo cách gọi AGY CLI trong:
  `/Volumes/DATA/Github/ToolbyMyself/model-plugin`
- Có thể tận dụng `ai-support-kit` tại:
  `/Volumes/DATA/Github/ToolbyMyself/ai-support-kit`
- Astra low chỉ dùng cho việc suy nghĩ về kiến trúc, phương hướng và các quyết định quan trọng; phải tiết kiệm token.

### 7.3. Điểm chưa chốt về AGY

AGY CLI là tiến trình chạy trong môi trường có CLI/runtime. Vercel không nên được giả định là có thể chạy trực tiếp tiến trình AGY cục bộ của máy người dùng. Prototype hiện tại gọi AGY qua server local nên chỉ xem là luồng phát triển/kiểm thử trên Mac.

Đã chốt: AGY chỉ dùng trong giai đoạn phát triển/code. AI trong sản phẩm là Gemini online, dùng OAuth phù hợp và độc lập với OAuth/credential của AGY CLI. Việc lưu DB Drive không phụ thuộc vào Gemini.

## 7.4. Hướng kết nối Gemini online

Luồng dự kiến cho bản Vercel:

```text
Safari iPad → Vercel web app → Vercel serverless AI proxy → Gemini API OAuth
```

- Frontend không chứa Gemini API key.
- OAuth/credential Gemini được quản lý ở phía serverless hoặc một dịch vụ trung gian an toàn.
- Frontend chỉ gửi ngữ cảnh bài học, câu hỏi và dữ liệu cần thiết; nhận câu trả lời từ proxy.
- Prompt hệ thống phải buộc AI phù hợp tuổi lớp 4, gợi ý theo tầng, không đưa đáp án ngay và phản hồi bài Văn theo rubric.
- Không dùng AGY CLI như runtime AI trực tiếp trong Vercel.
- Cần triển khai riêng bước cấu hình Google Cloud OAuth, quyền Gemini và biến môi trường Vercel trước khi bật AI production.

## 8. Kiến trúc đã chốt cho bản đầu

Đây là kiến trúc nhẹ đã chốt cho bản đầu:

```text
Vercel web app
  ├── UI học tập trên iPad
  ├── IndexedDB: dữ liệu cục bộ/offline
  ├── Google OAuth Drive: đồng bộ DB online visible file qua `drive.file`
  └── Vercel `/api/tutor`: Gemini online qua server-side OAuth; AGY chỉ là công cụ code/local fallback
```

Phương án Google Apps Script không thuộc luồng production đã chốt; thư mục `google-apps-script/` chỉ giữ làm tài liệu tham khảo cũ.

## 9. Quy trình phát triển

- Repository phải được khởi tạo bằng Git.
- Code cần được giao cho AGY sandbox khi phù hợp.
- Brief gửi AGY phải mô tả rõ:
  - đối tượng học sinh;
  - mục tiêu sư phạm;
  - giao diện mong muốn;
  - phạm vi được phép sửa;
  - kiến trúc đã được chốt;
  - tiêu chí kiểm thử;
  - những điều không được tự ý thay đổi.
- Mỗi thay đổi quan trọng phải được kiểm tra bằng test hoặc kiểm tra chạy thật trên trình duyệt.
- Ưu tiên dự án nhẹ, ít phụ thuộc, dễ chạy và dễ bảo trì.
- Không mở rộng sang tính năng lớn khi phần lộ trình học, lưu tiến độ và đồng bộ chưa ổn định.
- Khi phát hiện bất cập, người dùng sẽ trực tiếp sửa code hoặc yêu cầu AGY sửa code trong môi trường phát triển, sau đó commit/push lên GitHub.
- GitHub là nguồn mã nguồn chính; Vercel tự động tạo deployment/preview từ thay đổi đã push.
- Không cần xây dựng hàng đợi yêu cầu sửa code, AGY Bridge hoặc cơ chế AGY tự động commit từ trong app ở giai đoạn đầu.

## 10. Phạm vi giai đoạn đầu

### Bắt buộc

- Lộ trình Toán + Tiếng Việt lớp 3–4.
- Chẩn đoán nền lớp 3.
- Tiến độ học theo tuần/bài.
- AI trợ giảng tương tác chính cho Toán và Tiếng Việt/Văn.
- Giao diện dùng được trên iPad/Safari.
- Chạy trên Vercel.
- OAuth không dùng API key trong browser.
- Lưu và đồng bộ DB online qua Google Drive hoặc phương án được chốt tương đương.

### Nên có

- IndexedDB/offline cache.
- Gợi ý Toán theo nhiều tầng.
- Trợ lý AI phải đọc tiến trình/lỗi/ghi chú, đề xuất điều chỉnh phương pháp và nhịp học; người dùng có thể bấm áp dụng để lưu tuần cần ôn hoặc kế hoạch mới.
- Panel “Nền tính toán nhanh” 6 tuần đầu với nhịp luyện 8–10 phút mỗi buổi và theo dõi hoàn thành riêng.
- Chấm và phản hồi bài Văn theo rubric tuổi lớp 4.
- Lịch sử lỗi và nội dung cần ôn lại.
- PWA có thể mở như app trên iPad.

### Chưa cần ở giai đoạn đầu

- Nhiều tài khoản học sinh.
- Tài khoản giáo viên/lớp học.
- Thanh toán.
- Bảng xếp hạng.
- Hệ thống cộng tác thời gian thực.
- Backend DB riêng phức tạp.
- Tự động hóa toàn bộ việc ra đề/chấm điểm mà không có kiểm soát của phụ huynh.

## 11. Tiêu chí nghiệm thu

Sản phẩm được xem là đạt bản đầu khi:

1. Bách có thể mở web trên Safari iPad và bắt đầu học.
2. Có lộ trình Toán/Văn theo tuần, bắt đầu bằng phần kiểm tra nền lớp 3.
3. Mỗi bài có hướng dẫn từng bước, bài luyện và cách tự sửa.
4. Phụ huynh xem được tiến độ và lỗi cần ôn.
5. Dữ liệu vẫn còn sau khi đóng/mở lại trình duyệt.
6. Có thể đồng bộ DB lên Google Drive bằng OAuth, không lưu API key trong frontend.
7. Gemini là AI trợ giảng online chính; nếu Gemini tạm thời chưa cấu hình hoặc lỗi, chức năng học và lưu tiến độ vẫn hoạt động bình thường.
8. App chạy ổn trên Vercel và có hướng dẫn cấu hình OAuth domain/redirect rõ ràng.

## 12. Các quyết định đã chốt và triển khai

- [x] Dùng file nhìn thấy trong Google Drive qua scope `drive.file` (tên file: `Bach Learning DB.json`), không dùng `appDataFolder`.
- [x] Gọi Drive trực tiếp từ browser bằng Google Identity Services (GIS) token model với Client ID cấu hình linh hoạt (không lưu Client Secret ở client).
- [x] Gemini AI online là trợ giảng chính thức, gọi qua endpoint Vercel Serverless Function `/api/tutor` với OAuth refresh flow server-side (`GEMINI_CLIENT_ID`, `GEMINI_CLIENT_SECRET`, `GEMINI_REFRESH_TOKEN`, `GEMINI_PROJECT_ID`) và Google ID Token xác thực người dùng.
- [x] AGY là công cụ phát triển bên ngoài app; production handler trên Vercel không phụ thuộc vào `agy` binary. Local dev server giữ fallback `agy` CLI cho môi trường lập trình.
- [x] Dữ liệu local dùng IndexedDB schema version 2 (lưu progress, notes, chatHistory, updatedAt), đồng bộ Last-write-wins một người dùng kèm thời điểm sync.
- [x] Nhập giọng nói tiếng Việt bằng Web Speech API (`SpeechRecognition`/`webkitSpeechRecognition`) cho Safari iPad, nút bật/dừng rõ ràng, đưa transcript vào textarea để kiểm tra trước khi gửi.
- [x] PWA Service Worker (`sw.js`) chỉ cache tài nguyên tĩnh, tuyệt đối không cache API/token/DB riêng tư.
- [x] Giữ nguyên toàn bộ 36 tuần Toán + Văn, thang thử thách và rubric 0–3.

## 13. Phần Credential & Thao tác cấu hình thủ công còn lại

Để hệ thống đi vào hoạt động thực tế trên production, người dùng cần thực hiện các cấu hình sau (không thể tự động hóa hoàn toàn vì yêu cầu bảo mật tài khoản cá nhân):

1. **Google Cloud OAuth cho Gemini (Server-side):**
   - Tạo OAuth 2.0 Client ID loại Web Application trên Google Cloud Console.
   - Lấy Refresh Token có quyền gọi Generative Language API.
   - Thêm vào Vercel Environment Variables:
     - `GEMINI_CLIENT_ID`
     - `GEMINI_CLIENT_SECRET`
     - `GEMINI_REFRESH_TOKEN`
     - `GEMINI_PROJECT_ID`
     - `GOOGLE_TUTOR_CLIENT_ID`
     - `BACH_ALLOWED_EMAIL`
     - `GEMINI_MODEL` (tùy chọn, mặc định `gemini-2.5-flash`)
2. **Google Cloud OAuth cho Google Drive Sync (Client-side):**
   - Trên Google Cloud Console, tạo Web Client ID (hoặc dùng chung project nếu muốn).
   - Thêm Authorized JavaScript Origins: domain Vercel deployment (ví dụ `https://bach-learning.vercel.app`) và `http://localhost:4173`.
   - Lấy Web Client ID (dạng `*.apps.googleusercontent.com`) và nhập vào giao diện web qua nút "Kết nối Google Drive" (hoặc lưu trong biến môi trường build `window.BACH_GOOGLE_CLIENT_ID`). Client Secret **không** được nhập hay đưa vào frontend.
3. **Triển khai Git & Vercel:**
   - Commit các thay đổi và push lên remote repository GitHub.
   - Import repository vào Vercel để kích hoạt build và deploy tự động.
